# POC VRA_Controlador (ESP32) — TODO

Etapas da seção "Próximo passo imediato" do plano
(C:\Users\Pichau\.claude\plans\creio-que-como-complemento-swirling-snail.md).
Marcar [x] ao concluir; mover para [?] o que precisa de decisão do autor.

================================================================================
v1.0.0 FECHADA em 2026-04-30 (etapas 1-8 completas).
Próxima ação: etapa 9 (publicação no GitHub edcasag/VRA_Controlador).
Detalhes da próxima sessão no rodapé deste arquivo.
================================================================================

[x] 1. Inspecionar Receita.h e Calc.h do Syncat (D:\SynCAT\SynCAT\CNC\MINI\
       SyncatV3-Esp32\src\) apenas conceitualmente. Não copiar código nem
       nomes. Reusar somente conceitos do Cap. 6 da tese.
       Concluído 2026-04-29 — registro em versao.txt (Sessão Etapa 1).

[x] 2. Escrever do zero, com terminologia canônica da tese:
         - src/Geometria.h          (header-only: Ponto, Coord, projetar,
                                     polygonArea, pointInPolygon)
         - src/Kml.h / Kml.cpp      (parsing tinyxml2, Talhao, RegiaoPoligonal,
                                     RegiaoCircular, PontoReferencia,
                                     imprimirSumario, imprimirCsv)
         - src/LogicaHierarquica.h / LogicaHierarquica.cpp
                                    (Algoritmo 1 + IDW p=2, raio 100 m,
                                     d_min 0,5 m)
         - src/Pid.h                (header-only ~40 linhas)
       Concluído 2026-04-29 — paridade 1:1 com Python validada
       (vra_engine.dose_at, kml_parser.parse_kml). Detalhes em versao.txt.

[x] 3. Escrever os três entry points:
         - src/main_analise.cpp     (env:analise — sumário + CSV + 9 pontos demo)
         - src/main_poc.cpp         (env:poc — GPS + atuador + PID + 4 modos:
                                     exec, cal1, cal2, cal3 + tasks FreeRTOS)
         - src/main_tests.cpp       (env:tests — 9 testes cronometrados)
       Concluído 2026-04-29 — também extraído src/Atuador.h
       (TABELA_ABERTURA + posicao_alvo_para_taxa, compartilhado entre
       poc e tests). Detalhes em versao.txt.

[x] 4. Configurar platformio.ini com 3 envs:
         - analise (default), poc, tests
         - board esp32doit-devkit-v1, framework arduino, C++17
         - filesystem littlefs em analise e poc
       Concluído 2026-04-29 — também criado _esp32/.gitignore para
       artefatos de build PlatformIO. Filesystem littlefs habilitado
       também em tests (testes 3 e 9 leem Sitio_Palmar.kml). Detalhes
       em versao.txt.

[x] 5. Copiar dependências e dados:
         - tinyxml2 (vendored) em _esp32/lib/tinyxml2/
         - KMLs em _esp32/data/ (nomes com underscore: Sitio_Palmar.kml,
           ensaio_abcd.kml, talhao_completo.kml)
       Concluído 2026-04-29 — tinyxml2 zlib license (cabeçalho dos
       próprios arquivos satisfaz atribuição). library.json criado para
       o LDF do PlatformIO. KMLs copiados de _python/data/. Detalhes em
       versao.txt.

[x] 6. Compilar e fazer upload (autor):
         - pio run                              (analise)
         - pio run -e tests -t upload && monitor
         - capturar Serial → docs/sample_output_analise.txt
                              docs/sample_output_tests.txt
         - validação no hardware completo (env:poc) é opcional para v0.1
       Concluído 2026-04-29:
         - BUILD_ANALISE capturado em docs/sample_output_analise.txt
           (Sítio Palmar: 14 vértices, 10,85 ha, 6+1 polígonos, 2 círculos,
           7 pontos IDW + 9 pontos demo cobrindo os 7 cenários do Algoritmo 1).
         - BUILD_TESTS capturado em docs/sample_output_tests.txt
           (9 PASS, 0 FAIL). Latência ponta-a-ponta: média 73,4 µs,
           p95 92 µs — ~68× abaixo do critério ≤ 5 ms. Dado citável
           no Cap. 7 da tese.
         - 6 fixes de compilação (DEG_TO_RAD, std::clamp, LEDC 2.x,
           strtok_r, refator 3 envs→1 env) + 1 fix de teste (teste 5
           IDW simétrico: y projetado em vez de y=0 cru).
         - BUILD_POC ficou opcional para v0.1 (precisa GPS + atuador
           físico montados em bancada).

[x] 7. Validação cruzada Python ↔ ESP32:
         - rodar dose_at (Python) e dose (ESP32) para 5 pontos do
           Sitio_Palmar.kml; tolerância 1e-6
         - documentar em docs/cross_validation_python_vs_esp32.md
       Concluído 2026-04-30 — paridade 5/5 confirmada. Os 5 cenários
       atuais retornam valor exato por construção (4 binários +
       1 IDW degenerado em sample único). Validação numérica de IDW
       INTERPOLADO (múltiplos samples) fica coberta pelo modo SIM
       da próxima etapa, que percorre o talhão com trajetória densa.

[x] 8. BUILD_SIM — simulação completa standalone:
         - criar entry point main_sim.cpp (env: BUILD_SIM)
         - GPS sintético (trajetória boustrofédon dentro do talhão)
         - planta de 1ª ordem para o atuador (responde ao PID)
         - agrega erro por zona; relatório final estilo
           coverage_report.py do Python
         - marca BUILD_SIM como default no platformio.ini para que
           qualquer ESP32 DevKit V1 rode standalone, sem hardware
         - capturar saída em docs/sample_output_sim.txt
         - planejado para encerrar a v1.0.0 publicável.
       Implementação concluída 2026-04-30 (falta capturar saída no HW):
         - Python: scripts/export_trajectory_csv.py (~80 linhas);
           CSV ground truth gerado: data/trajetoria_ensaio_abcd.csv
           (2856 fixes, 190 KB, doses 0/60/75/90/100, vel 2,97-7,00 m/s).
         - ESP32 (~700 linhas C++): Terreno.h (declive + bossas),
           Planta.h (1a ordem TAU=0.3s), Trajetoria.h/.cpp (boustrofedon
           simplificado), CsvReader.h (streaming), MenuSerial.h/.cpp
           (lista *.kml e auto-detecta CSV), Relatorio.h/.cpp (agregador
           por zona + paridade Python<->ESP32), main_sim.cpp (orquestra),
           i18n.h (PT/EN via build flag).
         - platformio.ini: BUILD_SIM + LANG_PT default; LANG_EN comentado.
         - docs/README.md: nova seção BUILD_SIM com instruções de upload.
         - docs/cross_validation_python_vs_esp32.md: seção sobre validação
           numérica em massa via CSV ground truth.
       Concluído 2026-04-30:
         - 3 fixes pos-primeira-execucao no HW: (a) Kml::bbox() union
           para KMLs sem Field= (ensaio_abcd); (b) Trajetoria sem
           clipping de campo quando !talhaoCarregado; (c) MenuSerial
           sem LittleFS.exists() (logs vfs_api impossiveis de suprimir);
           (d) CsvReader::proximaLinhaComSpreading() para sincronizar
           sequencia ESP32 (sem arcos) com Python (com arcos spreading=0).
         - Captura final em docs/sample_output_sim.txt:
           - paridade Python<->ESP32: 2010/2010 pontos PASS,
             max |delta| = 0.00e+00 kg/ha (bit-a-bit identico).
           - validacao numerica em massa de IDW INTERPOLADO concluida.
           - latencia LogicaHierarquica: ~20 us/chamada.
           - latencia PID: ~5 us/iteracao.
         - Achados cientificos para a tese (Cap. 7/8):
           - erro de aplicacao -15% a -41% NAO vem do algoritmo (paridade
             PASS) — vem da inercia mecanica do servo (TAU=0.3s vs
             dt=0.2s entre fixes). Em transicoes de zona o setpoint muda
             antes da planta convergir.
           - vazamento residual ~5 kg/ha em "zona 0" (fora dos poligonos)
             durante fechamento do servo apos sair de zona com dose>0.
           - ambos os fenomenos sao realistas; o VRA_Simulador Python
             nao modela isso porque assume aplicacao instantanea da
             dose-alvo. POC ESP32 cumpre o papel de complementar.

[ ] 9. Publicação do repositório (PROXIMA SESSAO):
         - criar repo público novo edcasag/VRA_Controlador no GitHub web
           (autor faz; suficiente repo vazio, sem README)
         - configurar dual-repo via subtree push a partir de _esp32/
           (mesmo padrao usado com _python/ ↔ VRA_Simulador):
             git subtree push --prefix=_esp32 \
               git@github.com:edcasag/VRA_Controlador.git main
         - tag v1.0.0 + release notes no GitHub web
         - opcional: DOI Zenodo (precisa ativar webhook GitHub<->Zenodo
           antes da release; mesmo padrao do VRA_Simulador
           DOI 10.5281/zenodo.19893499)

       Pre-requisito antes da publicacao:
         - autor recompilar e capturar Sitio_Palmar standalone com bin
           de 5 kg/ha + notas autoexplicativas no rodape (fix da ultima
           sub-iteracao). Substituir placeholder na Captura 2 de
           docs/sample_output_sim.txt. Recompilar = 'pio run -t upload',
           KMLs/CSVs ja estao no FS.

[ ] 10. Link cruzado no VRA_Simulador (Python):
         - atualizar _python/README.md com link para VRA_Controlador
         - nota sobre complementaridade: Python = simulação algorítmica
           ideal; ESP32 = POC com atuador físico (abertura, PID, fator
           de escoamento, saturação por vazão máxima — não modelados no
           Python)

[ ] 11+. Melhorias futuras (v1.1+, opcionais):
         - boustrofedon completo no ESP32 (offset_polygon + headland_pass,
           +290 linhas C++) para Sitio Palmar irregular sem perda de
           cobertura nas quinas.
         - CSVs ground truth para Sitio_Palmar.kml e talhao_completo.kml
           (atualmente so ensaio_abcd tem CSV; rodar
           _python/scripts/export_trajectory_csv.py para gerar).
         - tunar PID/planta para reduzir erro de aplicacao (mas nota:
           erro alto eh achado cientifico — mostra a relevancia do POC
           ESP32 vs simulador Python idealizado).
         - testes automatizados que executem BUILD_SIM no CI
           (PlatformIO test runner ou QEMU ESP32).
