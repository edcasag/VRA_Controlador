# POC VRA_Controlador (ESP32) — log de progresso

Projeto:  POC do controlador VRA em ESP32 DevKit V1.
Versão:   v0.1 (em construção)
Autor:    Edson Casagrande (edscasag@gmail.com)
Início:   2026-04-29
Plano:    C:\Users\Pichau\.claude\plans\creio-que-como-complemento-swirling-snail.md
Repo:     edcasag/VRA_Controlador (a ser criado; dual-repo via subtree push,
          espelhando edcasag/VRA_Simulador, DOI 10.5281/zenodo.19893499).
Local:    _esp32/ (irmão de _python/)

Padrão (mesmo de _revisao/versao.txt da tese):
  Cada sessão registra:
    (a) arquivos modificados / criados
    (b) novas dependências (libs vendored, KMLs, etc.)
    (c) decisões tomadas
    (d) pendências para o autor

================================================================================
Sessão 2026-04-29 — Bootstrap do POC
================================================================================

(a) Arquivos criados:
    - _esp32/todo.txt    (9 etapas da seção "Próximo passo imediato" do plano)
    - _esp32/versao.txt  (este arquivo)

(b) Dependências:
    - nenhuma ainda (tinyxml2 e KMLs entram na etapa 5)

(c) Decisões tomadas:
    - confirmado escopo do plano: classes só Kml, LogicaHierarquica, Pid;
      sem IHM, BT, WiFi, OTA, display; 3 envs PlatformIO (analise default,
      poc, tests); KMLs em LittleFS com underscore no nome.
    - terminologia canônica do Cap. 6 da tese (Kml, RegiaoPoligonal,
      RegiaoCircular, PontoReferencia, LogicaHierarquica). Inspeção do
      Syncat será só conceitual.
    - leitura do Python (_python/src/main.py e _python/src/coverage_report.py)
      confirma a fórmula da vazão (taxa × largura × vel / 600 kg/min) e
      delimita o que o Python não modela: atuador, abertura, PID, fator de
      escoamento, saturação por vazão máxima — todos cobertos pelo POC ESP32.

(d) Pendências para o autor:
    - confirmar começo da etapa 1 (inspeção conceitual do Syncat) e etapa 2
      (início da escrita das classes Geometria.h, Kml, LogicaHierarquica, Pid).

================================================================================
Sessão 2026-04-29 — Etapa 1 (inspeção conceitual do Syncat)
================================================================================

(a) Arquivos modificados:
    - _esp32/todo.txt    (etapa 1 marcada como [x])
    - _esp32/versao.txt  (esta seção)

(b) Dependências: nenhuma.

(c) Conclusões da inspeção (sem cópia de código nem nomes):

    Receita.h/.cpp (Syncat — proprietário):
      Mistura tudo num objeto único. Estruturas misturando ponto isolado e
      perímetro, runtime calcula "ponto representativo" por região. Cinco
      variantes de métrica IDW (DW_POW, MANHATTAN, EUCLIDIANA, e versões
      ao quadrado). Quatro formatos de import (CSV/JSON/KML/GeoJSON) com
      convenções de nome por sufixo (R0_xx, RegionName=taxa). Limites
      estáticos (MAX_GEO_POINTS=1000, MAX_REGIONS=10). Lat/lon cruamente,
      sem projeção. Globais via ponteiro extern.
      → POC: separação limpa em Talhao / RegiaoPoligonal / RegiaoCircular /
        PontoReferencia, fiel à Tabela 4 do Cap. 6 da tese. Métrica IDW
        única (Euclidiana p=2, raio 100 m, d_min 0,5 m). Só KML. Sem
        globais (referência por parâmetro nas tasks). std::vector sem
        teto estático. Lat/lon projetados para x,y na entrada da
        LogicaHierarquica.

    Calc.h/.cpp:
      Implementa Fx = (Tx · V · L) / 600 em litros/min (líquido), com
      derivação dimensional documentada. Validação numérica: 150 L/ha,
      6 km/h, 14 m → 21 L/min ✓.
      → Confirma matematicamente a fórmula do plano. POC opera em kg/min
        (sólido), mesma matemática. Nenhuma divergência a resolver.

    SynPid.h:
      Multi-loop (MAX_PID_LOOPS=8), gain scheduling (gains_Normal/Strong),
      tabela linearização RPM↔PWM com 10 pontos, zona morta paramétrica,
      correção fuzzy, tipos POS/RPM, máquina de estados de calibração de
      válvulas (Vc_INIT, Vc_FIND_ZM, Vc_WAIT_RPM_ZM, ...) embutida no PID.
      Estrutura PID_ROT carrega ~30 campos.
      → POC: PID header-only ~40 linhas. Kp/Ki/Kd, dt, saturação min/max,
        anti-windup por clamping. Calibração é externa (3 rotinas no
        main_poc.cpp); resultado entra em constexpr no código.

    Exec.h:
      Sete modos (MONITOR, AUTO, PGM, MAN, CALIB, PROD, PAR), ~30 comandos
      via Bluetooth/teclas (x_Abr, x_Rec, x_Pid, x_Wifi, x_FC_Calibra etc.).
      → POC: 4 modos (EXEC, CAL1, CAL2, CAL3), comandos minimalistas via
        Serial USB ('m e', 'm c1', 'm c2', 'm c3'). Sem WiFi/BT/IHM.

(d) Pendências para o autor:
    - autorizar etapa 2: escrita do zero de Geometria.h, Kml.{h,cpp},
      LogicaHierarquica.{h,cpp}, Pid.h, com terminologia da tese.

================================================================================
Sessão 2026-04-29 — Etapa 2 (escrita das classes-base)
================================================================================

(a) Arquivos criados (todos em _esp32/src/):
    - Geometria.h            ~50 linhas, header-only
    - Pid.h                  ~30 linhas, header-only
    - Kml.h                  ~60 linhas
    - Kml.cpp               ~280 linhas
    - LogicaHierarquica.h    ~30 linhas
    - LogicaHierarquica.cpp  ~70 linhas
    Total: ~520 linhas C++17.

    Atualizados:
    - _esp32/todo.txt        (etapa 2 marcada como [x])
    - _esp32/versao.txt      (esta seção)

(b) Dependências introduzidas:
    - tinyxml2 (incluída via #include "tinyxml2.h"). Vendoring para
      _esp32/lib/tinyxml2/ acontece na etapa 5.
    - Arduino.h, FS.h, LittleFS.h, Print.h (já presentes no framework
      Arduino-ESP32).

(c) Decisões de design tomadas:

    1. Paridade matemática com Python (kml_parser.py + vra_engine.py):
       - ray-casting com termo "+ 1e-30" idêntico ao Python (evita NaN
         quando aresta é horizontal, yj == yi);
       - IDW: std::pow(d, potencia) em vez de d*d, mesmo com p=2 fixo
         (preserva paridade 1e-6 com Python; otimização do compilador
         resolve em release);
       - shoelace para polygonArea, Earth radius 6 371 000 m, projeção
         equiretangular local com origem fixada pelo primeiro lat/lon
         do KML — todos idênticos ao Python.

    2. Parser de nomes Tab. 4 sem std::regex:
       - regex pesa no ESP32 (compilação e tempo de match); usei find/
         strtod manualmente. Trata os 4 padrões: "Label=Rate:Radius m",
         "Rate:Radius m", "Label=Rate", "Rate". Sufixo "m" no raio é
         opcional.

    3. Print& como saída de imprimirSumario/imprimirCsv:
       - aceita Serial, File, custom Print. Flexível para o env "analise"
         (Serial) e futuros redirecionamentos. CSV com BEGIN/END como
         pede o plano.

    4. Talhao como struct única + flag talhaoCarregado():
       - espelha "field_polygon: Field | None" do Python sem optional/
         pointer global. Polígono sem tag de dose explícita é tratado
         como Talhao com dose=0 (ex.: contorno do Sítio Viçosa) — só se
         ainda não houver Field= explícito.

    5. Hierarquia em LogicaHierarquica::dose, ordem fielmente do Python:
       círculos -> polígonos exclusão -> polígonos inclusão (menor área
       vence) -> IDW -> talhão (dose-base) -> 0.

    6. Pid.h sem gains hardcoded:
       - construtor recebe Kp/Ki/Kd/dt/saida_min/saida_max. Os gains
         do plano (8.0, 1.5, 0.0, 0.1, -100, 100) ficam em main_poc.cpp
         (etapa 3).

(d) Pendências para o autor:
    - validar nomes contra a Tabela 4 do Cap. 6 da tese (Talhao,
      RegiaoPoligonal, RegiaoCircular, PontoReferencia) e a estrutura
      de classes (Kml, LogicaHierarquica, Pid). Ajustar se a Tab. 4
      usar terminologia ligeiramente diferente.
    - autorizar etapa 3: entry points main_analise.cpp, main_poc.cpp
      (com 4 modos exec/cal1/cal2/cal3 e tasks FreeRTOS) e main_tests.cpp
      (9 testes cronometrados).

================================================================================
Sessão 2026-04-29 — Etapa 3 (entry points)
================================================================================

(a) Arquivos criados (todos em _esp32/src/):
    - Atuador.h              ~70 linhas, header-only
                              (TABELA_ABERTURA, posicao_alvo_para_taxa,
                               abertura_em, posicao_para_abertura, AlvoAtuador)
    - main_analise.cpp      ~110 linhas (env: analise)
    - main_tests.cpp        ~270 linhas (env: tests, 9 testes cronometrados)
    - main_poc.cpp          ~330 linhas (env: poc)

    Atualizados:
    - _esp32/todo.txt        (etapa 3 marcada como [x])
    - _esp32/versao.txt      (esta seção)

    Total acumulado nas etapas 2+3: ~1300 linhas C++17 (estimativa do
    plano: ~1100; ligeiramente acima por causa do Atuador.h extraído e
    dos comentários docs).

(b) Dependências introduzidas:
    - nenhuma nova além das da etapa 2.

(c) Decisões de design tomadas:

    1. Atuador.h extraído como header novo (não previsto explicitamente
       no plano):
       - TABELA_ABERTURA, FATOR_ESCOAMENTO_KG_MIN, LARGURA_M,
         V_MIN_OPERACAO_KMH, abertura_em, posicao_para_abertura,
         posicao_alvo_para_taxa, AlvoAtuador.
       - Razão: testes 7 e 8 (env:tests) precisam dessa matemática sem
         depender de hardware. Extraindo, evita-se duplicação no
         main_tests.cpp e mantém DRY. As constantes que dependem de
         hardware (V_MIN, V_MAX) ficam em main_poc.cpp.

    2. AlvoAtuador struct retornando vazão, abertura, posição e flag
       de saturação:
       - permite log estruturado em main_poc.cpp e teste do flag
         "saturou" no env:tests.

    3. main_analise.cpp gera os 9 pontos demo a partir das próprias
       features do KML (centróides do talhão, regiões poligonais,
       circulares + variantes "fora" e "talhão+offset"). Funciona
       genericamente para Sítio Palmar, ensaio_abcd, talhao_completo;
       o autor lê a saída e valida os 7 cenários do Algoritmo 1.

    4. main_tests.cpp constrói KMLs sintéticos in-memory via
       carregarDoTexto() para os testes 1, 4 e 5. O teste 3 e 9 usam
       o Sítio Palmar real (precisa LittleFS montado e arquivo presente).
       Falha graciosa com [FAIL] se o KML não estiver no FS.

    5. main_poc.cpp arquitetura FreeRTOS:
       - task_controle no core 1 (50 Hz, periodo 20 ms via vTaskDelayUntil)
       - task_logica   no core 0 (2 Hz,  periodo 500 ms)
       - loop() drena fila_log, processa botões + comandos Serial,
         executa calibrações blocking quando solicitadas.
       - Variáveis compartilhadas (volatile uint8_t/float/bool, 32-bit
         atômico no ESP32) — sem mutex.

    6. NMEA parser inline (~30 linhas) só para $GPRMC/$GNRMC. Não usa
       lib externa (TinyGPS++ etc.) — minimalismo.

    7. Calibrações cal1/cal2/cal3 rodam blocking dentro de loop() com
       modo_ctrl = CTRL_IDLE / CTRL_MANUAL_AV / CTRL_MANUAL_REC para
       neutralizar o PID. Após terminar, volta para MODO_EXEC com ativo
       = false (operador precisa pressionar START).

    8. Comando Serial: "m e" / "m c1" / "m c2" / "m c3" — minimalista,
       parseado char-a-char. Outros comandos do plano não foram
       implementados (sem necessidade no POC).

    9. Critério 16 do plano (latência média ≤ 5 ms): bench no teste 9
       usa N=1000 fixes sintéticos espalhados em raio 200 m do
       centróide do talhão; reporta média + p95 e marca PASS se
       média ≤ 5000 µs. Reproduzível por terceiros, citável no Cap.7.

(d) Pendências para o autor:
    - autorizar etapa 4: configurar platformio.ini com 3 envs
      (analise default, poc, tests).
    - confirmar valores em Atuador.h (TABELA_ABERTURA, FATOR_ESCOAMENTO_KG_MIN,
      LARGURA_M, V_MIN_OPERACAO_KMH) — atualmente são placeholders do plano,
      a serem refinados após calibração de campo (cal1/cal2/cal3).

================================================================================
Sessão 2026-04-29 — Etapa 4 (platformio.ini + .gitignore)
================================================================================

(a) Arquivos criados:
    - _esp32/platformio.ini   (~30 linhas)
    - _esp32/.gitignore       (~15 linhas)

    Atualizados:
    - _esp32/todo.txt         (etapa 4 marcada como [x])
    - _esp32/versao.txt       (esta seção)

(b) Dependências introduzidas: nenhuma.

(c) Decisões de design tomadas:

    1. board = esp32doit-devkit-v1, framework = arduino, C++17 (gnu++17 para
       habilitar designated initializers e structured bindings se precisarmos
       em iterações futuras). monitor_speed = 115200 conforme plano.

    2. monitor_filters = esp32_exception_decoder na seção [env]:
       - aplica a todos os envs; útil para diagnosticar panics no
         poc (ex.: NMEA com formato inesperado).

    3. build_src_filter por env: cada main_* só compila no seu env, o
       restante (Geometria.h, Pid.h, Kml.{h,cpp}, LogicaHierarquica.{h,cpp},
       Atuador.h) é compartilhado nos três.

    4. board_build.filesystem = littlefs também no env:tests (não só
       analise/poc). Razão: testes 3 (área do Sítio Palmar) e 9 (bench
       de latência) carregam Sitio_Palmar.kml do FS.

    5. .gitignore mínimo: .pio/, .pioenvs/, .piolibdeps/ (build do
       PlatformIO), .vscode/, .idea/ (IDE), build/, *.elf, *.bin, *.map.
       Não inclui data/ — KMLs são versionados (entram na etapa 5).

(d) Pendências para o autor:
    - autorizar etapa 5: vendor de tinyxml2 em _esp32/lib/ e cópia dos
      KMLs (Sitio_Palmar.kml, ensaio_abcd.kml, talhao_completo.kml) para
      _esp32/data/ com nomes em underscore.

================================================================================
Sessão 2026-04-29 — Etapa 5 (vendor tinyxml2 + KMLs)
================================================================================

(a) Arquivos criados:
    - _esp32/lib/tinyxml2/tinyxml2.h     (74,463 bytes — original Lee Thomason)
    - _esp32/lib/tinyxml2/tinyxml2.cpp   (78,977 bytes — original Lee Thomason)
    - _esp32/lib/tinyxml2/library.json   (manifesto PlatformIO LDF)
    - _esp32/data/Sitio_Palmar.kml       (26,194 bytes — copiado de
                                          _python/data/Sitio Palmar.kml,
                                          renomeado com underscore)
    - _esp32/data/ensaio_abcd.kml         (1,558 bytes)
    - _esp32/data/talhao_completo.kml     (3,019 bytes)

    Atualizados:
    - _esp32/todo.txt                     (etapa 5 marcada como [x])
    - _esp32/versao.txt                   (esta seção)

(b) Dependências introduzidas:
    - tinyxml2 (Lee Thomason, www.grinninglizard.com), licença zlib
      (cabeçalho dos próprios arquivos satisfaz atribuição). Não-MIT,
      mas permissiva equivalente — compatível com publicação do
      VRA_Controlador como software livre.

(c) Decisões de design tomadas:

    1. Vendoring direto dos 2 arquivos do tinyxml2-master usado pelo
       Syncat (D:/SynCAT/SynCAT/CNC/MINI/SyncatV3-Esp32/lib/tinyxml2-master/),
       pasta renomeada para tinyxml2/ (sem sufixo "-master"). Mesma
       versão validada em produção.

    2. library.json mínimo: name, version="vendored", license="Zlib",
       homepage do upstream, frameworks="*" e platforms="*". srcDir="."
       para LDF não precisar inferir.

    3. KMLs com underscore no nome (evita escape em comandos Serial e
       no LittleFS.open). Sitio Palmar.kml -> Sitio_Palmar.kml.

    4. Sanity check do Sitio_Palmar.kml:
       - <Polygon> count = 8 (1 talhão Palmar + 6 inclusão + 1 exclusão Sede)
       - <Point> count   = 10 (2 círculos + 7 amostras + 1 LookAt? a
                               diferença de 1 fica para a etapa 6 — a
                               saída do imprimirSumario vai validar).
       - 14 entradas no polígono Palmar (13 vértices únicos + fechamento)
         => bate com criterio 4 do plano.
       - Polígonos com espaços em torno do "=" (ex.: "Sede = 0",
         "Cana = 120") são tolerados pelo trimCopia do parser.

(d) Pendências para o autor:
    - autorizar etapa 6: compilar e fazer upload (analise + tests no
      mínimo); capturar saída Serial em docs/sample_output_analise.txt
      e docs/sample_output_tests.txt. O autor opera o hardware (PlatformIO
      + ESP32 + cabo USB).

================================================================================
Sessão 2026-04-29 — Etapa 6 (compilacao): fix conflito DEG_TO_RAD
================================================================================

(a) Arquivos modificados:
    - _esp32/src/Geometria.h
        DEG_TO_RAD -> GRAUS_PARA_RAD (4 ocorrencias).
        Comentario explicando o porque.

    Atualizados:
    - _esp32/versao.txt   (esta entrada)

(b) Dependencias: nenhuma.

(c) Diagnostico do erro de compilacao reportado pelo autor:
    Arduino.h (framework-arduinoespressif32) define DEG_TO_RAD como
    macro:
        #define DEG_TO_RAD 0.017453292519943295769236907684886
    Quando main_analise.cpp inclui Arduino.h antes de Geometria.h, a
    macro substitui textualmente o identificador do constexpr no header,
    gerando "expected unqualified-id before numeric constant".

    Correcao: renomeado o constexpr para GRAUS_PARA_RAD (PT-BR, alinhado
    com o estilo do projeto). Documentado no proprio header.

    Outros nomes conferidos (sem colisao):
      - std::max/std::min sao usados qualificados; ESP32-Arduino nao
        define min/max como macros (isso eh uma quirk do AVR).
      - F("..."), digitalRead/Write, ledcAttach/Write, analogRead* etc.
        sao do Arduino e estao corretamente usados.

(d) Pendencia para o autor:
    - rodar 'pio run' novamente (env: analise) e reportar saida.

--------------------------------------------------------------------------------
Sub-iteracao etapa 6 — fix std::clamp (env: tests)
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/src/Pid.h        (helper inline clamp_d via std::min/std::max)
    - _esp32/src/Atuador.h    (passa a usar clamp_d, inclui Pid.h)

(b) Diagnostico: env:analise compilou OK (RAM 6,6%, Flash 27,6%); env:tests
    falha porque a libstdc++ do toolchain-xtensa-esp32 8.4.0 nao expoe
    std::clamp (ausencia documentada na libstdc++ stripada do Xtensa).
    GCC suporta C++17 mas o header <algorithm> da libstdc++ vendored no
    toolchain nao tem std::clamp.

    Correcao: clamp_d(v, lo, hi) = std::min(std::max(v, lo), hi),
    inline em Pid.h, reusada em Atuador.h. Comentario na origem
    documentando o porque.

(c) Pendencia para o autor:
    - rodar novamente 'pio run -e tests' e depois 'pio run -e poc'
      para fechar o ciclo de fixes de compilacao.

--------------------------------------------------------------------------------
Sub-iteracao etapa 6 — fix env: poc (LEDC API antiga + strtok_r)
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/src/main_poc.cpp
        * std::strtok_r -> strtok_r (POSIX, nao std::).
        * ledcAttach(pin, freq, res) [API 3.x] -> ledcSetup(canal, freq, res)
          + ledcAttachPin(pin, canal) [API 2.x].
        * Adicionados constexpr LEDC_CH_AVANCAR = 0, LEDC_CH_RECUAR = 1.
        * pwm_avancar/pwm_recuar/pwm_zerar agora chamam ledcWrite com
          o canal LEDC, nao com o pino (era a forma 3.x).

(b) Diagnostico: framework-arduinoespressif32 @ 3.20017.241212 corresponde
    a Arduino-ESP32 2.0.17 (versionamento PlatformIO com offset). API LEDC
    moderna (ledcAttach/ledcWrite por pino) so existe a partir do 3.x.
    A API antiga (ledcSetup + ledcAttachPin + ledcWrite por canal) eh
    estavel desde o 1.x; usei essa.

(c) Pendencia para o autor:
    - rodar 'pio run -e tests' (ja deve estar OK apos o fix de std::clamp)
      e 'pio run -e poc' (com este fix). Reportar resultado.

--------------------------------------------------------------------------------
Sub-iteracao etapa 6 — refator: 1 env unico + macro -DBUILD_X
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/platformio.ini      (3 envs -> 1 env unico [env:vra]; modo
                                  selecionado via build_flags com 3 linhas
                                  alternativas comentadas)
    - _esp32/src/main_analise.cpp  (envolto em #ifdef BUILD_ANALISE / #endif
                                    + #pragma message para feedback no build)
    - _esp32/src/main_tests.cpp    (idem com BUILD_TESTS)
    - _esp32/src/main_poc.cpp      (idem com BUILD_POC)

(b) Razao da mudanca:
    Pedido do autor por simplicidade — 'pio run -e <env>' nao era
    intuitivo para quem nao usa PlatformIO regularmente. Com a
    refatoracao, o autor:
      1. Edita platformio.ini, descomenta UMA das 3 linhas de build_flags.
      2. Roda sempre o mesmo comando: 'pio run' (ou icone "✓" do
         PlatformIO no canto inferior esquerdo do VSCode).
      3. O #pragma message imprime no console qual modo esta sendo
         compilado.

    Nao ha trade-off de funcionalidade: cada main_*.cpp continua sendo
    arquivo independente; o #ifdef apenas decide qual produz setup()/loop()
    no link final.

(c) Pendencia para o autor:
    - rodar 'pio run' com BUILD_ANALISE ativo (ja eh o default no novo
      platformio.ini). Confirmar que compila e o pragma aparece no console.
    - depois trocar para BUILD_TESTS, rodar 'pio run', confirmar.
    - depois BUILD_POC, idem.

--------------------------------------------------------------------------------
Sub-iteracao etapa 6 — confirmacao final: 3 modos compilam
--------------------------------------------------------------------------------

(a) Resultado:
    - BUILD_ANALISE  -> SUCCESS (RAM 6,6% / Flash 27,6%)
    - BUILD_TESTS    -> SUCCESS (RAM 6,6% / Flash 28,0%)
    - BUILD_POC      -> SUCCESS (autor confirmou)

    O #pragma message identifica claramente o modo no console de build.
    Fixes de compilacao acumulados nesta etapa:
      1. DEG_TO_RAD -> GRAUS_PARA_RAD (conflito com macro do Arduino.h)
      2. std::clamp -> clamp_d via min/max (libstdc++ stripada do Xtensa)
      3. ledcAttach (3.x) -> ledcSetup + ledcAttachPin (2.x)
      4. ledcWrite por pino -> por canal (API 2.x)
      5. std::strtok_r -> strtok_r (POSIX, nao std::)
      6. Refator: 3 envs -> 1 env unico + macro -DBUILD_X (UX)

(b) Pendencia para o autor:
    - upload do firmware:    pio run -t upload
    - upload dos KMLs no FS: pio run -t uploadfs
    - monitor:               pio device monitor
    - capturar saida em _esp32/docs/sample_output_<modo>.txt
      Procedimento detalhado em _esp32/docs/README.md.

--------------------------------------------------------------------------------
Sub-iteracao etapa 6 — fix teste 5 (IDW simetrico) + captura BUILD_TESTS
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/src/main_tests.cpp   (teste_5_idw passa ym projetado em vez
                                   de y=0 cru; comentario explicando)
    - _esp32/docs/sample_output_tests.txt  (criado, saida 9/9 PASS)
    - _esp32/todo.txt             (etapa 6 marcada [x])
    - _esp32/versao.txt           (esta sub-iteracao)

(b) Diagnostico do FAIL inicial do teste 5:
    Primeira execucao no hardware: 8 PASS, 1 FAIL no teste 5 (IDW
    simetrico, esperado 150.0). Causa: o teste passava y=0.0 cru para
    logica.dose(xm, 0.0), mas pos.x/pos.y dos pontos de referencia
    estao PROJETADOS em metros com origem no primeiro vertice do Field
    (equiretangular local). Como o Field comeca em lat=-0.005, y=0
    (lat=0) projetado fica em ~+555 m, longe dos pontos P1 e P2
    (pos.y ~ +555 m). A distancia (~555 m) excede o raio IDW (100 m),
    nenhum ponto contribui, e a funcao cai no fallback Field=0.

    Fix: passar tambem ym = (p1.y + p2.y) / 2.0 (media projetada).
    Mesmo padrao ja usado corretamente no teste 4 (cenario IDW).

(c) Resultado apos fix (sample_output_tests.txt):
    9 PASS, 0 FAIL.
      - parser KML 7 padroes: 3,9 ms (1x na carga)
      - pointInPolygon 8 casos: 13,9 us/op
      - polygonArea (quadrado + Sitio Palmar 10,85 ha): 29 us/op
      - LogicaHierarquica::dose 7 cenarios: 44,6 us/op
      - IDW simetrico (P1=100, P2=200, ponto medio): 150,0 (PASS)
      - PID degrau 0->100: convergiu em <= 20 iter
      - tabela posicao<->abertura (identidade 1e-6): 16 us/op
      - canonico (100 kg/ha, 5 km/h, 20 m): 20 us/op
      - latencia ponta-a-ponta N=1000: media 73,4 us, p95 92 us.
        Criterio do Cap. 7 (<= 5 ms): atendido com folga ~68x.

    Numero de latencia (73,4 us / 92 us p95) eh dado bruto reproduzivel
    por terceiros, citavel no Cap. 7 da tese sem dependencia do Syncat
    proprietario.

(d) Pendencia para o autor:
    - opcional: capturar BUILD_POC com hardware completo (GPS + atuador
      em bancada). Nao bloqueia v0.1 — etapa 6 fica fechada com analise
      + tests.
    - autorizar etapa 7: validacao cruzada Python <-> ESP32 (5 pontos
      do Sitio Palmar, dose_at vs dose, tolerancia 1e-6, documentar em
      docs/cross_validation_python_vs_esp32.md).

================================================================================
Sessao 2026-04-30 — Etapa 7 (validacao cruzada Python <-> ESP32)
================================================================================

(a) Arquivos criados:
    - _esp32/docs/cross_validation_python_vs_esp32.md  (~80 linhas)

    Atualizados:
    - _esp32/todo.txt    (etapa 7 marcada [x]; etapa 8 nova: BUILD_SIM;
                          publicacao deslocada para 9, link cruzado para 10)
    - _esp32/versao.txt  (esta sessao)

(b) Dependencias: nenhuma.

(c) Resultado:
    Paridade 5/5 confirmada. Os 5 pontos cobrem 5 cenarios distintos
    do Algoritmo 1 (talhao IDW single-sample, polig inclusao Poor->Good,
    polig inclusao Nobre, polig exclusao Sede, circulo exclusao). Origem
    da projecao identica nos dois lados:
        Python: lat=-23.3521368 lon=-46.9454401  (primeiro vertice Field)
        ESP32:  lat=-23.3521368 lon=-46.9454401  (primeiro vertice Field)

    Notas registradas no markdown:
    - 4/5 cenarios sao "binarios por construcao" (retornam r.dose ou 0
      sem aritmetica fracionaria) — paridade trivial por design.
    - 1/5 (centroide do talhao) cai em IDW mas com 1 sample dentro do
      raio, degenerando para o valor literal do sample.
    - Validacao numerica rigorosa de IDW INTERPOLADO (multiplos samples
      contribuindo, dose fracionaria) fica coberta pelo BUILD_SIM da
      proxima etapa.

(d) Decisao do autor:
    - encerrar etapa 7 sem nova captura de hardware com 6 casas decimais
      (paridade visual e suficiente para os cenarios atuais).
    - planejar etapa 8 NOVA: BUILD_SIM — simulacao COMPLETA num ESP32
      DevKit V1 standalone, sem nenhum periferico externo. GPS sintetico
      (trajetoria boustrofedon dentro do talhao) + planta de 1a ordem
      para o atuador respondendo ao PID + agregacao de erro por zona +
      relatorio final estilo coverage_report.py do Python. Esse modo
      vira o DEFAULT publicavel da v1.0.0.

(e) Pendencia para o autor:
    - autorizar plano detalhado da etapa 8 (BUILD_SIM) — proposta
      apresentada no chat antes de codar.

================================================================================
Sessao 2026-04-30 — Etapa 8 (BUILD_SIM, implementacao)
================================================================================

(a) Arquivos criados:

    Python:
    - _python/scripts/export_trajectory_csv.py    (~100 linhas)

    Dados:
    - _esp32/data/trajetoria_ensaio_abcd.csv      (2856 fixes, 189 KB,
                                                   doses 0/60/75/90/100,
                                                   vel 2,97-7,00 m/s,
                                                   t_sim ~9,4 min)

    ESP32 (todos em _esp32/src/, ~700 linhas C++17):
    - Terreno.h          ~75 linhas, declive linear + bossas Gaussianas
                          (espelha terrain.py 1:1)
    - i18n.h             ~80 linhas, ~30 macros PT/EN por build flag
    - Planta.h           ~30 linhas, 1a ordem TAU=0.3s
    - CsvReader.h        ~100 linhas, streaming linha-a-linha LittleFS
    - Trajetoria.h       ~50 linhas
    - Trajetoria.cpp     ~110 linhas, boustrofedon simplificado
                          (faixas E-O com clipping pointInPolygon)
    - MenuSerial.h       ~40 linhas
    - MenuSerial.cpp     ~95 linhas, lista *.kml + auto-detecta CSV
    - Relatorio.h        ~55 linhas
    - Relatorio.cpp      ~95 linhas, agregador por zona + paridade
    - main_sim.cpp       ~210 linhas, orquestra menu + loop completo

    Atualizados:
    - _esp32/platformio.ini      (BUILD_SIM + LANG_PT default; LANG_EN
                                  comentado; comentarios atualizados)
    - _esp32/docs/README.md      (nova secao BUILD_SIM com instrucoes
                                  + como adicionar KMLs novos + como
                                  trocar idioma)
    - _esp32/docs/cross_validation_python_vs_esp32.md
                                  (seção sobre validacao numerica em
                                   massa via CSV ground truth)
    - _esp32/todo.txt             (etapa 8 marcada [/], pendente captura)
    - _esp32/versao.txt           (esta sessao)

(b) Dependencias introduzidas: nenhuma (reusa tinyxml2 ja vendored).

(c) Decisoes de design tomadas (alem das ja registradas no plano):

    1. Conversao vel m/s -> km/h dentro do main_sim.cpp:
       - Trajetoria emite fixe.vel em m/s (compativel com Python).
       - posicao_alvo_para_taxa(rate, vel_kmh) espera km/h.
       - Conversao explicita: vel_kmh = fixe.vel * 3.6.

    2. PID a 50 Hz (dt=0.02s) com gains do main_poc.cpp (8.0/1.5/0.0).
       Numero de passos PID entre fixes: dt_fixe / 0.02, clipped em [1, 50].
       Em fixes muito longos (saltos de y_atual entre faixas), o cap em 50
       evita aquecer demais sem afetar o estado estacionario.

    3. Trajetoria simplificada cobre QUALQUER KML do LittleFS:
       - Boustrofedon E-O com step_m=1m, largura 20m.
       - Clipping por pointInPolygon nos extremos de cada faixa
         (suficiente para retangulos; perde area em quinas irregulares).
       - Headland_pass e offset_polygon ficam para v1.1.

    4. CSV streaming evita carregar 190 KB em RAM:
       - CsvReader le linha-a-linha via File.readStringUntil('\n').
       - Sequencia esperada Python = sequencia gerada pela Trajetoria
         (ambos: faixas y crescente, direcao alterna, gnss_noise=0).
       - Se sequencias divergirem (ex.: KML diferente do CSV), a paridade
         vai dar FAIL imediatamente — comportamento desejado.

    5. Relatorio agrega por chave de dose (round 1 casa) em vez de por
       label da zona:
       - Mais robusto: zonas com mesma dose mas labels diferentes
         (ex.: 'Good=100' e 'Nova=100') somam juntas, refletindo o que
         o atuador efetivamente fez.
       - Zona 0 (curvas / fora do talhao) eh ignorada na ponderacao
         global de erro %.

(d) Pendencia para o autor:
    - rodar 'pio run' (default ja eh BUILD_SIM + LANG_PT). Esperado:
      SUCCESS, RAM <15%, Flash <35%.
    - rodar 'pio run -t upload' + 'pio run -t uploadfs' (envia KMLs +
      CSV ground truth) + 'pio device monitor'.
    - escolher opcao 1 (ensaio_abcd.kml) no menu Serial.
    - capturar saida completa em _esp32/docs/sample_output_sim.txt.
    - confirmar veredicto PASS na seção "Paridade Python<->ESP32".

--------------------------------------------------------------------------------
Sub-iteracao etapa 8 — fixes pos-primeira-execucao no HW
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/src/Kml.h, Kml.cpp     (novo metodo bbox() union de features)
    - _esp32/src/Trajetoria.cpp     (usa kml.bbox(); sem clipping de campo
                                     quando !talhaoCarregado)
    - _esp32/src/main_sim.cpp       (chama kml.bbox(); remove erro fatal
                                     em "KML sem Talhao"; usa
                                     proximaLinhaComSpreading; mostra
                                     so o nome do CSV em vez de "0 fixes")
    - _esp32/src/CsvReader.h        (novo proximaLinhaComSpreading que
                                     pula linhas spreading=0)
    - _esp32/src/MenuSerial.cpp     (single-pass: lista todos os arquivos
                                     do raiz, separa em KMLs + set de
                                     CSVs disponiveis. Sem LittleFS.exists)

    Atualizados:
    - _esp32/docs/sample_output_sim.txt   (captura final do BUILD_SIM)
    - _esp32/todo.txt                     (etapa 8 marcada [x])
    - _esp32/versao.txt                   (esta sub-iteracao)

(b) Diagnostico dos 3 problemas da primeira execucao no HW:

    1. ERRO "[ERRO] KML sem Talhao (Field=)":
       ensaio_abcd.kml so tem 4 poligonos A=90, B=75, C=60, D=100. Nenhum
       eh Field=Rate. Python KmlData.bbox() faz fallback union (talhao
       se existir + zonas + circulos + pontos). ESP32 nao fazia.
       Fix: Kml::bbox() espelha o Python; Trajetoria usa esse bbox e
       roda sem clipping de campo nesse caso. main_sim aceita KMLs sem
       Talhao explicito.

    2. LOGS "[E][vfs_api.cpp:105] open(): does not exist":
       arduino-esp32 emite isso via printf direto sempre que LittleFS.
       exists() falha. esp_log_level_set("vfs_api", ESP_LOG_NONE) NAO
       captura porque nao eh ESP_LOG.
       Fix: MenuSerial::descobrir() lista TODOS os arquivos num pass,
       separa em KMLs + std::set<csv>. Lookup no set em vez de exists.

    3. PARIDADE FAIL (max |delta|=100, 1434/2010 divergencias):
       Python boustrophedon emite ~94 fixes adicionais por curva em U
       na cabeceira (arc_n = max(40, int(pi*radius*3/step))). Esses
       fixes vem com spreading=0 (distribuidor desligado). ESP32
       simplificado pula direto para a proxima faixa, sem gerar arcos.
       Resultado: sequencias dessincronizam apos faixa 1 (CSV avanca
       100 linhas no arco enquanto ESP32 esta na proxima faixa).
       Fix: CsvReader::proximaLinhaComSpreading pula spreading=0 do
       CSV, sincronizando ambos os lados em fixes "espalhando".

(c) Resultado final (sample_output_sim.txt):

    Paridade Python <-> ESP32 (2010 pontos):
      max |delta_dose|: 0.00e+00 kg/ha
      divergencias > 1e-3 kg/ha: 0
      veredicto: PASS

    Validacao numerica em massa de IDW INTERPOLADO concluida com
    paridade BIT-A-BIT (Python double vs ESP32 float identicos para
    2010 pontos cobrindo as 4 zonas A/B/C/D). Esse era o gap que
    a etapa 7 nao cobria (5 pontos isolados, todos com valor exato
    por construcao). Justifica encerrar a v1.0.0 publicavel.

    Latencias medidas:
      - LogicaHierarquica::dose: ~20 us/chamada
      - Pid::atualizar:           ~5 us/iteracao

(d) Achados cientificos para a tese (Cap. 7 Resultados / Cap. 8 Discussao):

    1. Erro de aplicacao alto NAO vem do algoritmo:
       Zona A (90 kg/ha) -> aplicado 57.28 (-36.35%)
       Zona B (75 kg/ha) -> aplicado 51.33 (-31.56%)
       Zona C (60 kg/ha) -> aplicado 50.45 (-15.91%)
       Zona D (100 kg/ha)-> aplicado 58.75 (-41.25%)
       Erro % global ponderado: 31.25%

       Como a paridade da LogicaHierarquica eh PASS, o erro vem da
       inercia mecanica do atuador. Modelo da planta: TAU=0.3s (1a
       ordem); dt entre fixes = step/v ≈ 0.2s. Resposta da planta em
       0.2s = 1 - exp(-0.2/0.3) = 49% do alvo. Em transicoes de zona
       (90->75, 60->100) o setpoint muda antes da planta convergir.

    2. Vazamento residual em "zona 0" (canto NE do bbox, fora dos 4
       poligonos): aplicado_medio = 4.90 kg/ha em 100 m². Sao 5 fixes
       onde dose_esp=0 mas o servo ainda esta fechando a partir de
       posicao residual da zona anterior.

    3. Ambos os fenomenos sao REALISTAS — o VRA_Simulador Python nao
       os modela porque assume aplicacao instantanea da dose-alvo
       (sem PID, sem inercia, sem histerese). POC ESP32 cumpre o
       papel especificado no plano: complementar o Simulador Python
       com a fisica do atuador.

(e) Pendencia para o autor:
    - autorizar etapa 9 (publicacao do repo edcasag/VRA_Controlador no
      GitHub, dual-repo via subtree push, mesmo padrao do
      VRA_Simulador). Autor decide se essa etapa eh agora ou em outra
      sessao.
    - opcional: ajuste fino do PID/planta para melhorar erro de
      aplicacao. Mas nota cientifica: erro alto eh um achado, nao um
      bug — mostra a relevancia do POC ESP32 para a tese.

--------------------------------------------------------------------------------
Sub-iteracao etapa 8 — bin de 5 kg/ha + captura Sitio Palmar standalone
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/src/Relatorio.cpp           (chaveZona usa bin de 5 kg/ha)
    - _esp32/docs/sample_output_sim.txt  (adiciona Captura 2: Sitio
                                          Palmar standalone, latencia
                                          ~71 us/chamada da Logica
                                          Hierarquica)
    - _esp32/versao.txt                  (esta sub-iteracao)

(b) Diagnostico do bug "51 zonas no relatorio do Sitio Palmar":
    chaveZona = round(dose * 10) / 10 estava bin de 0.1 kg/ha. Para
    KMLs com IDW interpolado (Sitio Palmar tem 7 samples gerando
    valores fracionarios continuos como 115.3, 115.5, 115.6, 113.5...),
    cada interpolacao virava uma "zona" nova com 1 fixe (20 m²).
    Resultado: 51 linhas no relatorio, ilegivel.

    Fix: chaveZona = round(dose / 5) * 5 (bin de 5 kg/ha). Agrupa
    interpolacoes da mesma vizinhanca de samples; em ensaio_abcd
    (doses inteiras 60/75/90/100) nao agrega nada.

(c) Achado adicional do Sitio Palmar standalone (Cap. 7 da tese):

    Latencia LogicaHierarquica::dose: ~71,4 us / chamada
    em cenario realista com hierarquia COMPLETA:
      - 1 talhao Field (14 vert, 10.85 ha)
      - 6 poligonos de inclusao (Poor, Good, Nobre, Normal, Nova, Cana)
      - 1 poligono de exclusao (Sede)
      - 2 circulos de exclusao
      - 7 pontos de referencia IDW

    Folga de ~70x sobre o criterio <= 5 ms do plano. Numero citavel
    no Cap. 7 da tese (junto com o ~20 us do ensaio_abcd e o bench
    sintetico de 73 us do BUILD_TESTS).

    Erro de aplicacao tambem alto no Sitio Palmar (33.17% global),
    confirmando o achado da Captura 1: o gargalo eh o servo, nao o
    algoritmo.

(d) Pendencia para o autor:
    - rodar 'pio run -t upload' (so firmware, KMLs/CSVs ja no FS).
    - 'pio device monitor', escolher opcao 1 (Sitio Palmar).
    - capturar o relatorio com bin de 5 kg/ha e colar substituindo
      o placeholder na Captura 2 de docs/sample_output_sim.txt.
    - se OK, autorizar etapa 9 (publicacao GitHub).

================================================================================
Sessao 2026-04-30 — Etapa 9 (publicacao no GitHub)
================================================================================

(a) Arquivos criados (em _esp32/, todos para o repo publico):
    - LICENSE                         (MIT, identico ao _python/LICENSE)
    - README.md                       (PT-BR + English, ~13 KB)
    - CITATION.cff                    (metadados + bibref SBIAGRO 2025)
    - docs/SBIAGRO2025_artigo.pdf         (550 KB, copiado de _referencia/)
    - docs/SBIAGRO2025_apresentacao.pdf   (2.2 MB, copiado de _referencia/)

    Atualizados:
    - _esp32/todo.txt                 (etapa 9 marcada [x])
    - _esp32/versao.txt               (esta sessao)

(b) Dependencias: nenhuma (so artefatos de doc).

(c) Operacoes git executadas (a partir do toplevel da tese):
    1. git add _esp32/{LICENSE,README.md,CITATION.cff,docs/SBIAGRO2025_*.pdf}
    2. git commit -m "POC VRA_Controlador: README + LICENSE + CITATION + SBIAGRO 2025 (etapa 9)"
       Resultou em commit ea79f05 no main da tese.
    3. git subtree split --prefix=_esp32 -b vra-controlador-export
       Criou branch local com 15 commits do POC, com _esp32/ promovido
       para a raiz. Commit final da split: 5b893de.
    4. git push https://github.com/edcasag/VRA_Controlador.git \
              vra-controlador-export:main --force
       Force-push substituiu o commit auto-gerado do LICENSE inicial
       do GitHub (8955534) pelo nosso historico completo (5b893de).
    5. git tag v1.0.0-vra-controlador vra-controlador-export
       git push <repo> refs/tags/v1.0.0-vra-controlador:refs/tags/v1.0.0
       Tag v1.0.0 criada no remoto publico.

    Verificacao final do remoto (git ls-remote):
      HEAD              = 5b893de
      refs/heads/main   = 5b893de
      refs/tags/v1.0.0  = 5b893de

(d) Decisoes tomadas:
    1. PDFs do SBIAGRO 2025 incluidos em docs/ (artigo + apresentacao).
       Difere do padrao do _python/ (que nao leva PDFs do CEA), mas faz
       sentido aqui porque o artigo SBIAGRO descreve exatamente o
       controlador deste repo. Decisao registrada na mensagem do commit.

    2. Tag local v1.0.0-vra-controlador (sufixada) para evitar colisao
       com futuras tags v1.0.0 da tese ou do _python/. No remoto publico
       aparece como v1.0.0 simples.

    3. Force-push autorizado pelo autor — prática convencional para
       repo recém-criado com unico commit auto-gerado pelo GitHub
       (LICENSE), sem colaboradores nem stars/forks externos.

(e) Pendencias para o autor:
    - autorizar etapa 10: link cruzado no _python/README.md apontando
      para VRA_Controlador (e nota sobre complementaridade — texto
      simetrico ao que ja temos no README do VRA_Controlador).
    - opcional: limpar branch local 'vra-controlador-export' apos
      consolidar (git branch -D vra-controlador-export). Recomendo
      manter por ora — proxima sessao de sync (mais commits no _esp32/)
      pode reusar com 'git subtree split' incremental.

--------------------------------------------------------------------------------
Sub-iteracao etapa 9 — DOI Zenodo + badge no README
--------------------------------------------------------------------------------

(a) Arquivos modificados:
    - _esp32/CITATION.cff   (campo 'doi:' + entrada em 'identifiers:')
    - _esp32/README.md      (badge DOI dinamico no topo + bloco
                             "Como citar" reescrito com Concept DOI,
                             BibTeX e nota sobre Version DOI)

(b) DOIs atribuidos pelo Zenodo:
    - Concept DOI (estavel, sempre aponta pra ultima versao):
        10.5281/zenodo.19922431
        Usado em CITATION.cff, badge do README e bloco principal de
        cita\xe7\xe3o. Convencao alinhada com a recomenda\xe7\xe3o do Zenodo.
    - Version DOI da v1.0.0 (especifico, imutavel):
        10.5281/zenodo.19922432
        Mencionado no README como "DOI da versao especifica" para
        citar a v1.0.0 deterministicamente em artigos futuros.

(c) Operacoes git:
    1. git commit -m "POC VRA_Controlador: DOI Zenodo no CITATION.cff
       e badge no README (etapa 9)" -> commit 586c6d7 no main da tese.
    2. git subtree push --prefix=_esp32 \
       https://github.com/edcasag/VRA_Controlador.git main
       Push fast-forward (5b893de..70dbb8f) — sem --force porque o
       remote agora tem nosso historico.

(d) Diagnostico do webhook GitHub<->Zenodo (registrado para futura
    referencia, ja que o autor encontrou bug de cache no toggle):
    - Repo novo (criado depois do primeiro OAuth do Zenodo) NAO aparece
      no toggle list mesmo apos 'Sync now'. Workaround: revoke do
      Zenodo em github.com/settings/applications + logout + relogin
      no Zenodo via GitHub. Apos re-OAuth o repo aparece.
    - Webhook do Zenodo dispara 3 eventos por release no GitHub
      (created, published, released); Zenodo so aceita o 'published'
      (status 202), os outros dois retornam 409. Isso eh comportamento
      normal — nao indicar problema no log.
    - Apos publicar a release (gh release create), o DOI sai em
      ~1-2 minutos. Aparece em https://zenodo.org/me/uploads.

(e) Repo publico final:
    - https://github.com/edcasag/VRA_Controlador
    - main = 70dbb8f (HEAD)
    - tag v1.0.0 = 5b893de (commit pre-DOI; documentacao da release
      v1.0.0 captura o estado original publicado, sem o badge).
      Nao requer republicacao — quem cita pelo DOI 19922432 (Version)
      ou 19922431 (Concept) chega no arquivo Zenodo correto.
    - Concept DOI: 10.5281/zenodo.19922431
    - Version DOI: 10.5281/zenodo.19922432

(f) Pendencias para o autor:
    - (resolvido na sessao seguinte — ver "Sessao etapa 10".)

================================================================================
Sessao 2026-04-30 — Etapa 10 (cross-link _python <-> _esp32 + fix DOI _python)
================================================================================

(a) Arquivos modificados (em _python/, fora do diretorio do POC):
    - _python/CITATION.cff   (DOI Version 19893499 -> Concept 19893498
                              em 'doi:' e 'identifiers:'; descricao
                              "Concept DOI" agora bate com o valor)
    - _python/README.md      (badge dinamico apontando pro Concept DOI;
                              4 ocorrencias do Version DOI substituidas
                              pelo Concept; bloco "Veja tambem" / "See
                              also" adicionado em pt e en com link pro
                              VRA_Controlador, DOI Concept 19922431, e
                              nota de uma frase sobre complementaridade)

(b) Operacoes git:
    1. git commit -m "VRA_Simulador: DOI Concept + cross-link para
       VRA_Controlador" -> commit 42e030d no main da tese.
    2. git subtree push --prefix=_python \
         https://github.com/edcasag/VRA_Simulador.git main
       Push fast-forward (6f630fc..18d24c0) — sem --force.

(c) Decisoes tomadas:
    - Cross-link textual curto (1 frase) na introducao de cada lado,
      em vez de secao dedicada de complementaridade. Razao: o README
      do _esp32 ja tem secao completa com tabela; replicar do lado
      _python alongaria um README ja extenso. Uma frase + link basta
      pra navegacao do leitor.
    - Mantido o DOI principal como Concept (estavel) em badge,
      cita\xe7\xe3o e BibTeX. Version DOI especifico nao mencionado no
      _python (pra simplificar; quem precisa cita versao especifica
      acessa pelo Zenodo direto).

(d) Estado final dos repos publicos:

    edcasag/VRA_Controlador (ESP32):
      main = 70dbb8f
      tag v1.0.0 = 5b893de
      Concept DOI = 10.5281/zenodo.19922431
      Version DOI v1.0.0 = 10.5281/zenodo.19922432

    edcasag/VRA_Simulador (Python):
      main = 18d24c0
      tag v1.0.0 = (existente)
      Concept DOI = 10.5281/zenodo.19893498
      Version DOI v1.0.0 = 10.5281/zenodo.19893499

(e) Pendencias para o autor:
    - nenhuma. v1.0.0 dos dois repos publicada com DOIs corretos
      (Concept estavel + Version imutavel) e cross-links bilaterais.
      Etapas 1-10 do plano fechadas em 2026-04-30.

================================================================================
Sessao 2026-04-30 — Imagens da tese nos repos publicos
================================================================================

(a) Arquivos criados/modificados:

    Imagens copiadas de Dissertacao_Mestrado/images/ com nomes
    normalizados (underscore-snake-case):

    _esp32/images/  (~1 MB total)
      - arquitetura_sistema.png         diagrama de blocos
      - distribuidor_duplo_atuadores.jpg visao geral concreta (KML +
                                         GPS + ESP32 + atuadores)
      - controlador_vra_pcb.jpg         render 3D da placa EC-1.0
      - esquema_prototipo.jpg           esquematico completo
      - calibracao.jpg                  montagem fisica
      - efeito_bull_eye.jpg             ilustracao IDW (nao
                                         referenciada no README, fica
                                         disponivel no repo p/ futuro)

    _python/assets/  (~700 KB total)
      - simulador_vra_1.jpg             ensaio A/B/C/D em andamento (PT)
      - simulador_vra_2.jpg             ensaio com painel de relatorio
                                         final (PT)
      - vra_simulator_1.jpg             ensaio em andamento (EN, nao
                                         referenciado)
      - vra_simulator_2.jpg             ensaio com relatorio final (EN)

    READMEs atualizados:
      _esp32/README.md
        - imagem hero (distribuidor_duplo_atuadores) apos badges
        - nova secao "Arquitetura do sistema" (arquitetura_sistema)
        - nova secao "Hardware" (PCB + esquematico + calibracao)
        - entrada images/ na arvore de "Estrutura do repositorio"
      _python/README.md
        - simulador_vra_1.jpg apos tabela de "KMLs incluidos"
        - simulador_vra_2.jpg apos tabela do "Lendo o relatorio"
        - vra_simulator_2.jpg apos secao English "Report"
        - screenshot_palmar.png mantido como hero

(b) Operacoes git:
    1. git commit -m "Imagens da tese nos repos publicos: arquitetura,
       PCB e screenshots" -> commit no main da tese.
    2. git subtree push --prefix=_esp32 \
         https://github.com/edcasag/VRA_Controlador.git main
       Push fast-forward (70dbb8f..71eb9a2).
    3. git subtree push --prefix=_python \
         https://github.com/edcasag/VRA_Simulador.git main
       Push fast-forward (18d24c0..8c00ad7).

(c) Decisoes:
    - Imagens normalizadas para underscore-snake-case (alinhado com
      o padrao dos KMLs em data/ e dos arquivos de codigo).
    - 2 imagens copiadas mas nao referenciadas no README:
      efeito_bull_eye.jpg (no _esp32) e vra_simulator_1.jpg (no
      _python). Mantidas no repo para uso futuro (artigo, expansao
      do README, etc.) — overhead pequeno (~200 KB combinados).
    - Nao foram trazidas para os repos: imagens genericas do diretorio
      Dissertacao_Mestrado/images/ que nao agregam aos repos publicos
      (Fertilizer.jpg, VRA - Trator.jpg, trator.jpg, Sitio Vicosa.JPG,
      Talhao KML*.JPG). Ficam restritas ao diretorio da tese.

(d) Estado final dos repos publicos:
    - edcasag/VRA_Controlador main = 71eb9a2 (HEAD)
    - edcasag/VRA_Simulador  main = 8c00ad7 (HEAD)
    - tags v1.0.0 inalteradas em ambos.

(e) Pendencias para o autor:
    - nenhuma.
